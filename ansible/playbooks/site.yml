---
- name: Complete infrastructure setup and deployment
  hosts: webservers
  become: yes
  vars_files:
    - "{{ playbook_dir }}/../group_vars/all.yml"

  roles:
    - common
    - docker
    - web_app
